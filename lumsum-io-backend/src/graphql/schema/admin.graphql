type AdminAuth {
  accessToken: String!
  refreshToken: String!
  admin: Admin!
}

type Admin {
  id: ID!
  name: String!
  email: EmailAddress!
  access: String!
  lastLoggedin: String
}

input AdminInput {
  email: EmailAddress!
  password: String!
  name: String!
}
